//Wurde aus intro.ts umgewandelt

/*"use strict";
const contentElement = getContentElement();
if (contentElement.type === "image") {
    contentElement.src;
}
else {
    contentElement.text;
}
function getContentElement() {
    if (Math.random() >= 0.5) {
        const returnObj = { type: "image", src: "https://..." };
        return returnObj;
    }
    else {
        const returnObj = { type: "text", text: "text" };
        return returnObj;
    }
}*/
